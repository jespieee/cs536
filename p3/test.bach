boolean a.
boolean b.
void c.
integer d.
struct test [
    integer a.
    boolean b.
    void c.
]
void funcNoParam[] [
    integer a.
    boolean b.
    void c.
    struct test d.
]
void funcParam[integer a, integer b] [
    integer a.
    boolean b.
    void c.
    integer d.
    a = d.
    b = ^d.
    a++.
    a--.
    return.
    return c.
    binary = "z".
    binary = true.
    binary = false.
    binary = a > 1.
    binary = a < 1.
    binary = a >= 1.
    binary = a <= 1.
    binary = a == a.
    binary = a ^= a.
    binary = c + 1.
    binary = c - 1.
    binary = c * 1.
    binary = c / 1.
    binary = c & c.
    binary = c | c.
    unary = ^c.
    unary = -c.

    while (binary) {
        a = a + 1.
        b = b * 3 + 1.
    }
]



if (b) { x = y. } else { x = c. }
%% asd comment
# asd comment
